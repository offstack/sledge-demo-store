{
  "name": "liquid",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start:css": "postcss src/index.css -o assets/tailwind.css",
    "start": "npm run start:css && theme deploy --allow-live && theme watch --allow-live",
    "build": "NODE_ENV=production npm run start:css && theme deploy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@tailwindcss/forms": "^0.5.6",
    "autoprefixer": "^10.4.15",
    "postcss-cli": "^10.1.0",
    "tailwindcss": "^3.3.3"
  }
}